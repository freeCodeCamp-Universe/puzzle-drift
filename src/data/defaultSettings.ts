import type { GameSettings } from '../types/game';

export const DEFAULT_SETTINGS: GameSettings = {
  highContrast: false,
  musicEnabled: true,
  reducedMotion: false,
  soundEnabled: true,
  theme: 'rift-dark',
};
